export enum UserRole {
  EMPLOYEE = "employee",
  MANAGER = "manager",
  HR = "hr",
  SUPER_ADMIN = "super_admin"
}

export enum CodePurpose {
  REGISTER = "register",
  RESET_PASSWORD = "reset_password"
}
